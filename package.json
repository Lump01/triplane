{
  "name": "triplane",
  "version": "0.1.0",
  "description": "Cross-platform 3D object scanning components for React Native (ARKit/ARCore) and Web (WebXR/photogrammetry capture)",
  "license": "MIT",
  "private": false,
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "react-native": "dist/native/index.js",
  "source": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepare": "npm run build"
  },
  "peerDependencies": {
    "react": ">=17",
    "react-native": ">=0.72"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "typescript": "~5.8.3"
  }
}
